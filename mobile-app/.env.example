# Optional: override API base for dev/staging EAS builds.
# Production default is https://api.grandhr.in/api (see app.json extra.apiUrl).
# Copy to .env (not committed) or set in Expo: eas env:create --name EXPO_PUBLIC_API_URL
EXPO_PUBLIC_API_URL=https://api.grandhr.in/api
